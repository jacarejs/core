import { appHref } from '../app-base.js'
import { t } from '../i18n.js'

export <view>
  <section class="page">
    <header class="page-hero">
      <p class="page-kicker">${t('notFoundPage.kicker')}</p>
      <h1 class="page-title">${t('notFoundPage.title')}</h1>
      <p class="page-lead">
        ${t('notFoundPage.lead1')} <code>nav.js</code>${t('notFoundPage.lead2')}
      </p>
    </header>

    <a class="btn" jacare-go="/" href=${appHref('/')}>${t('notFoundPage.back')}</a>
  </section>
</view>
