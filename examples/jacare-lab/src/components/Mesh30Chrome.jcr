import { t, translate } from '../i18n.js'

export <view>
  <div class="mesh30-screen" data-screen="chrome">
    <header class="mesh30-screen-head">
      <span class="mesh30-screen-label">${t('bag.mesh30Chrome.screenLabel')}</span>
      <span class="mesh30-badge mesh30-badge-address" :title=${translate('bag.mesh30Chrome.badgeTitle')}>
        ${@lab-mesh30/count} ${t('bag.mesh30Chrome.inCart')}
      </span>
    </header>
    <p class="mesh30-screen-lead">${t('bag.mesh30Chrome.lead')}</p>
    <button type="button" class="btn btn-ghost" on-click=${@lab-mesh30/clear}>
      ${t('bag.mesh30Chrome.clear')}
    </button>
  </div>
</view>
